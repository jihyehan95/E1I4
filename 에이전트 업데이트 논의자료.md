# 에이전트 업데이트 논의자료 (2026-08-25 팀 논의용)

## 배경

`.claude/agents/` 아래 6개 에이전트(plan → route → schedule → budget → document, orchestrator)의 구조는 [기획서 초안.md](기획서 초안.md) 시절 초기 설계 그대로다. 반면 최근 커밋들에서 기획적으로 바뀐 부분이 많은데, 그 변화의 상당수가 에이전트 지시문(`.claude/agents/*.md`)이 아니라 `orchestrator/call-agent` 서버 로직과 `딸깍 서비스.html` 프론트엔드에만 반영돼 있다. 그 결과 **Claude Code에서 orchestrator를 직접 실행하는 경로**(README "1) Claude Code로 실행")와 **화면(`딸깍 서비스.html`)으로 실행하는 경로**(README "2) 화면으로 실행")가 서로 다르게 동작하는 지점이 생겼다.

아래는 이 갭을 다시 좁히기 위해 내일 팀에서 결정해야 할 안건들이다.

## 발견된 불일치

### 1. 자사 양식 업로드가 plan 에이전트를 완전히 우회함
- 관련 커밋: `40084b8`(시작화면 기본양식/자사양식 구분, 자사양식 AI 파싱 입력칸 추가), `c16e2d6`(자사 양식 업로드 시 항목 그대로 동적 입력칸 생성)
- 이 기능은 `orchestrator/call-agent`의 `/parse-template` 엔드포인트에 완전히 별도의 하드코딩 스키마(`TemplateParseZ`)로 구현돼 있다.
- `plan.md`는 여전히 표준 UI 입력 항목만 전제로 서술돼 있어서, Claude Code로 orchestrator를 직접 부르면 이 기능 자체를 쓸 수 없다.

### 2. 예산 승인/반려 게이팅이 프론트엔드 로컬 상태로만 존재함
- 관련 커밋: `d224db3`(예산 승인/반려)
- "예산 초과 시 승인해야 document 결과를 볼 수 있다"는 로직(`BUDGET_APPROVAL`)이 브라우저 로컬 상태일 뿐 서버로 전송되지 않는다.
- `budget.md`/`orchestrator.md`에는 이 승인 절차가 반영돼 있지 않다. orchestrator.md의 "반려·요청·경고·조정필요 시 사용자 확인 후 진행" 원칙과 사실상 같은 성격인데도 파이프라인 레벨 로직으로 편입되지 않은 상태.

### 3. route/schedule 통합 여부가 여전히 미결
- [기획서 초안.md](기획서 초안.md)의 "막힌 점"에 있던 항목이 지금도 그대로 미결 — 두 에이전트는 계속 분리돼 있다.
- route(동선·후보 조사·유류비 계산)와 schedule(시간표 생성)의 책임은 명확히 갈려 있고, budget → route 반려 피드백 루프가 이미 잘 동작하는 것으로 보여 굳이 합칠 이유는 약해 보이지만, 팀 확인 필요.

### 4. 확장 비전(출장 → 모든 업무) 처리 방향 불명확
- 기획서 초안에는 "출장뿐 아니라 일정·장소·비용·문서가 필요한 모든 업무(사업/행사/여행/회식 등)로 확장"하는 비전이 있었다.
- 현재 에이전트들(plan의 "출장비 규정", document의 "품의서" 등)은 출장에 특화돼 있어, 이 비전을 지금부터 반영해 범용화할지, 출장 파이프라인 안정화 후로 미룰지 방향이 필요.

### 5. (참고) 그 외 기획 변화 중 에이전트 정의는 안 건드린 것들
- `251c35c` 필수 입력 최소화(정책 B) + 포켓몬 애니메이션 실시간 동기화, `dbabd92` 리브랜딩+경비 기준 설정, `1afbcec` 폼 리디자인+보고서 템플릿 첨부, `bd41381` 실제 지도(네이버 NCP Maps)·애니메이션 — 대부분 UX/인프라 성격이라 에이전트 지시문과 무관해도 되는 것들로 보이지만, 팀에서 "에이전트가 알아야 할 정보인지" 한 번씩 짚고 넘어가는 게 안전.
- 참고로 `d224db3`, `251c35c`는 여러 에이전트 `.md`와 HTML/call-agent를 함께 수정한 드문 사례로, 기획-에이전트 동기화가 실제로 이뤄진 선례다.

## 내일 결정할 안건

| # | 안건 | 검토 방향(초안) |
| --- | --- | --- |
| 1 | 자사 양식 업로드를 plan 에이전트의 정식 입력 계약(표준 항목 + 동적 커스텀 항목)으로 흡수할지, 지금처럼 화면 전용 기능으로 둘지, 별도 전처리 에이전트로 분리할지 | plan 계약 확장 쪽이 두 실행 경로 정합성엔 유리 |
| 2 | 예산 승인/반려 게이팅을 budget 에이전트 출력 + orchestrator 로직으로 승격할지, 화면 전용 UX로 유지할지 | 파이프라인 레벨 승격 쪽이 orchestrator.md 원칙과 일관적 |
| 3 | route/schedule을 통합할지, 분리 유지할지 | 분리 유지가 무난해 보이나 팀 확인 필요 |
| 4 | 확장 비전(출장 → 전 업무)을 지금부터 설계에 반영할지, 출장 파이프라인 안정화 후로 미룰지 | 우선 출장 파이프라인 정합성부터 |
| 5 | 안건 5의 UX/인프라 변화 중 에이전트가 알아야 할 항목이 있는지 팀별로 재확인 | 팀원별 담당 에이전트 기준으로 체크 |

## 참고

- 조사 근거: `.claude/agents/*.md`, `orchestrator/call-agent`, `딸깍 서비스.html`, `기획서 초안.md`, `README.md`, `KNOWN-ISSUES.md`, `git log`

# 오늘 작업 분담 계획 (2026-08-25, 4인)

이 문서는 [에이전트 업데이트 논의자료.md](에이전트 업데이트 논의자료.md)에서 짚은 "화면 UI에만 있고 에이전트 지시문엔 없는 기능들"을 정리하기 위한 오늘 하루치 작업 분담이다. 작업은 `refactor/agent-form-branching` 브랜치에서 진행하고, 그룹별로 기능 브랜치를 따로 파서 작업한 뒤 이 브랜치로 합친다.

## 오늘의 목표

**데모 프로토타입 완성 및 시현.** 로컬 실행(Claude Code 또는 로컬 8788 서버)을 영상으로 촬영하는 발표자료 수준이 목표이고, 실제 배포는 하지 않는다.

## 공통 결정사항

- **실행 범위**: 각자 로컬에서 실행하는 사내 도구로 유지. 서버 인증·네트워크 바인딩 강화, 동시 실행 시 캐시 충돌(`STAGE_CACHE_PATH`) 수정은 오늘 범위 아님 — 다음 스프린트로 이월.
- **예산 게이팅 규칙**: 예산이 **초과일 때만** 사람이 승인/반려를 결정하는 대기 상태로 멈춘다(기존 `call-agent`의 `fail` 상태를 그대로 재사용 — 새 상태값을 따로 만들지 않음). **적정이면 자동으로 document까지 진행**한다.
  - 단, 지금 백엔드엔 "승인해서 초과 건을 그대로 진행"시키는 경로가 아예 없다(화면 승인 버튼이 서버에 아무것도 안 보내고 화면 잠금만 풂). 이번에 `form.budgetApproved` 플래그를 추가해서, 승인 시 document까지 이어서 실행되도록 만든다.
- **자사양식 업로드 크기 제한**: 10MB (초과 시 413 응답)
- **`call-agent` 동시 수정**: Group 1(게이팅 로직)과 Group 4(네이버 연동)가 같은 파일을 건드리므로 각자 `git worktree`로 작업 디렉터리를 분리한다. **병합 순서: Group 1 → Group 4** (Group 4는 Group 1이 병합된 이후 최신 상태에서 이어서 작업).
- **`딸깍 서비스.html` 동시 수정**: Group 3(예산 승인 UI 영역)과 Group 4(지도 영역)가 서로 다른 구역을 건드리지만, 병합 전에 서로에게 알리고 순서를 맞춘다.
- **오늘 안 하는 것**: 서버 인증/바인딩 보완, 동시성(캐시 충돌) 수정, route/schedule 통합 여부 결정, 확장 비전(출장 → 전 업무) 설계.

## 그룹별 작업

### Group 1 — orchestrator 라우팅/게이팅

- **담당 파일**: `.claude/agents/orchestrator.md`, `orchestrator/call-agent`(라우팅 부분)
- **브랜치**: `feat/orchestrator-branching-gating`
- **할 일**:
  1. 기본양식/자사양식 분기를 `orchestrator.md`에 정식 지시문으로 명시.
  2. `call-agent`에 `form.budgetApproved` 플래그 추가 — budget 단계가 `fail`(초과)이어도 이 플래그가 있으면 하드스톱 없이 document까지 이어서 실행. 플래그가 없으면 지금처럼 멈춘다(적정이면 원래도 자동 통과라 변경 불필요).
- **완료 확인**: 예산 초과 케이스에서 승인 플래그를 주면 document까지 실행되는지 확인.

### Group 2 — plan 에이전트 계약 확장

- **담당 파일**: `.claude/agents/plan.md`
- **브랜치**: `feat/plan-customform-contract`
- **할 일**:
  1. 자사양식(`customForm`/`customFormAnswers`) 입력을 plan의 정식 "받는 것"으로 문서화 — 지금은 `call-agent`에만 하드코딩돼 있고 `plan.md`엔 이 경로 자체가 없음.
  2. call-agent가 미리 채운 실측 좌표(Naver Geocoding 결과)가 있으면 그대로 쓰고, 없는 목적지만 추정하도록 반영.
- **완료 확인**: 자사양식 샘플 입력으로 plan 단독 실행 확인.

### Group 3 — 예산 승인 게이팅

- **담당 파일**: `.claude/agents/budget.md`, `.claude/agents/document.md`, `딸깍 서비스.html`(예산 승인 UI 영역, 약 1799행·1855-1897행·2332행 부근)
- **브랜치**: `feat/budget-approval-gating`
- **할 일**:
  1. `budget.md`에 "초과 = 승인 대기, 적정 = 자동 통과" 규칙 명시.
  2. `document.md`에 "승인되어 넘어온 초과 건은 초과 사실을 문서에 표시" 처리 추가.
  3. 화면: 적정이면 승인 버튼 없이 바로 document 열람 가능하게 바꾸고, 초과일 때만 승인/반려 버튼을 노출. 승인 클릭 시 `budgetApproved: true`를 실어 서버(`/run`)를 재호출하도록 연결(지금은 화면 잠금만 풀고 서버로 아무것도 안 보냄).
- **완료 확인**: 초과 케이스에서 승인 버튼이 실제 서버 재호출로 이어지는지, 적정 케이스는 버튼 없이 바로 열리는지 확인.

### Group 4 — route/schedule 정합성 + 네이버 지도

- **담당 파일**: `.claude/agents/route.md`, `.claude/agents/schedule.md`, `orchestrator/call-agent`(네이버 연동), `딸깍 서비스.html`(지도 영역, 약 8-10행·962-964행·1126-1144행·2382-2404행)
- **브랜치**: `feat/route-naver-maps`
- **할 일**:
  1. `call-agent`에 Naver Geocoding/Directions 15 호출 함수 + 서버 프록시 엔드포인트 추가. `NAVER_MAP_CLIENT_SECRET`은 절대 브라우저로 노출하지 않고 서버에만 유지(REST API는 HMAC 서명에 secret이 필요).
  2. 자사양식 업로드 요청에 10MB 크기 제한 추가(413 응답).
  3. `route.md`가 call-agent로부터 받은 실측 거리·이동시간이 있으면 그대로 쓰고, 없을 때만 추정하도록 수정.
  4. `schedule.md`의 이동시간 재계산 로직이 route 결과와 중복되는 부분을 정리 — route가 이미 계산한 값을 재사용.
  5. 화면 지도를 Leaflet+OSM → 네이버 지도 JS SDK로 교체(장소 검색도 Nominatim 대신 서버 프록시 경유로 변경).
- **완료 확인**: 지도에 실제 핀·동선이 표시되는지, route와 schedule의 이동시간이 서로 일치하는지 확인.

## 마무리 (전 그룹 완료 후)

1. 기본양식 경로, 자사양식 경로 각각 end-to-end 1회 실행 검증.
2. 시연용 영상 촬영.

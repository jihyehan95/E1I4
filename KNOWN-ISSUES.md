# 알려진 이슈 / 확인 필요 사항

## (해결됨) 폼에 다른 값을 입력해도 샘플 결과가 계속 표시되던 문제

**증상이었던 것:** `딸깍 서비스.html`에서 입력값(지역·방문 대상·예산 등)을 바꿔도, AI 서버가 꺼져 있으면 결과가 항상 `flowpilot-data.js`에 하드코딩된 제주 현장심사 샘플로만 나왔다.

**조치:** `flowpilot-data.js`의 샘플 데이터를 제거하고 `window.FLOWPILOT_DATA = null`로 비웠다. 이제 AI 서버가 꺼져 있을 때는 `buildReport()`의 폼 입력 기반 폴백이 그대로 사용된다(단, AI가 계산하지 않으므로 근사치이며 임의 주소는 좌표가 없어 지도 핀은 못 꽂는다). AI 서버(`node orchestrator/call-agent --serve`)가 켜져 있으면 이전과 동일하게 실제 파이프라인 결과가 반영된다.

> 기록일: 2026-08-19 · 해결일: 2026-08-19

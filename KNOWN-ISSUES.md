# 알려진 이슈 / 확인 필요 사항

## 1. 폼에 다른 값을 입력해도 샘플(제주 현장심사) 결과가 계속 표시됨

**증상:** `딸깍 서비스.html`에서 입력값(지역·방문 대상·예산 등)을 바꿔도, 결과(계획서·일정·경비·지도·담당 클릭 상세)가 항상 `flowpilot-data.js`의 샘플 데이터로만 나온다.

**원인:**
- 화면 렌더는 폼 입력이 아니라 `window.FLOWPILOT_DATA`(= `flowpilot-data.js`에 하드코딩된 제주 샘플)를 읽는다.
- 입력값이 결과에 반영되는 경로는 **orchestrator(실제 AI 파이프라인)가 실행될 때뿐**이다.
  - AI 서버 ON: 버튼 → `http://localhost:8788/run` 호출 → 5단계가 폼 입력으로 새 데이터 생성 → 반영됨.
  - AI 서버 OFF(현재, API 미사용): 호출 실패 → 폴백 `buildReport()` 실행 → 이 함수 첫 줄
    `if (window.FLOWPILOT_DATA) { return buildReportFromData(window.FLOWPILOT_DATA); }`
    때문에 **폼 입력을 무시하고 무조건 샘플을 렌더**한다. (담당 클릭 상세/지도도 전부 `FLOWPILOT_DATA` 기준)

**즉, 이 폴백을 고치지 않으면 API 없이 실행하는 한 계속 첨부된 샘플 내용이 뜬다.**

**고치려면 (택1, 추후 논의):**
- (a) 서버 OFF 시 폼 입력으로 계획서/방문지/기본 일정·예산표를 생성하도록 폴백 수정. 단 AI가 없으므로 근사치이며, 임의 주소는 좌표가 없어 지도 핀은 못 꽂음(알려진 주소만).
- (b) (a) + 무료 지오코딩(Nominatim, 키 불필요)으로 입력 주소를 좌표로 변환해 지도 핀·동선까지 반영. 네트워크 의존·주소 정확도에 따라 실패 가능.
- (c) 샘플은 시연용 완성 예시로 그대로 두고, 입력 반영은 실제 API(orchestrator) 실행 시에만. 화면에 "샘플 미리보기"임을 명시.

> 기록일: 2026-08-19 · 이 브랜치의 UI 변경(지도·포켓몬 애니메이션·인라인 상세)과는 별개의 사전 확인 사항.
